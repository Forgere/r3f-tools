# 2026-09-04

- 完成 `factory-conveyor-plan.md`：风格（参考图1 toy/pastel 圆润风）、布局（参考图2 多线交汇工厂）、组件策略、动画清单、性能目标。
- 新增 `examples/VividFactoryConveyorExample.tsx`：5 条 pastel 输送线（自定义 shader 箭头流动材质）、CuteRack 圆角货架、InstancedMeshPool 货箱流动、Leva 控制面板（速度/货量/线路显隐/4 机位）、暖光 + PCF 软阴影。`examples/main.tsx` 增加 `/vivid-factory` 路由。
- **Bug 修复**：R3F 中 `shadow-camera={{ left, right, ... }}` 对象写法会把 `light.shadow.camera` 替换为普通对象，导致运行时 `DirectionalLightShadow.updateMatrices` 报 `Cannot read properties of undefined (reading 'copy')`。正确写法是逐项 prop：`shadow-camera-left={-15}` 等。已修复并 typecheck 通过。
- 注意：WorkBuddy Windows 侧无法跑 Vite build（Rollup 原生二进制不匹配），预览需在 WSL 内 `npm run example`。
- **合流设备**：在 `VividFactoryConveyorExample.tsx` 中针对 `(-1.2, 0, -0.2)` 的 H 交汇点新增 `CuteMergeJunction` 组件。通过 `clipPathForJunction` 将三条输送线在交汇点前后缩回 `MERGE_GAP=0.85`，用圆形平台 + 导向护栏 + 中心导流帽 + 控制盒盖住原先的重叠， cargo 仍走原曲线通过。已类型检查通过。
- **迭代 v2**：① 第二交汇点 S `(8, 0.7, 0)` 新增 `CuteSorterJunction` 分流设备（摆动拨臂 sin 动画、扫码拱门+闪烁灯、护栏），`JUNCTIONS` 数组统一管理，clip 对两个交汇点都生效。② 输送线模型优化：带面抬升 `BELT_HEIGHT=0.7`（蓝线床 1.3）、`SupportLegs` 支撑腿（曲线采样 1.35 间距、避开交汇设备、InstancedMeshPool 渲染）、`CuteMotor` 驱动电机（风扇随速度旋转）、两台交汇设备加落地支撑柱、相机预设 target 抬高。
- **重要坑**：`InstancedMeshPool` 的 `mesh.count` 初始为 0 且组件内部不自动同步——使用方必须显式调 `setInstanceCount(n)`，否则实例完全不渲染（此前 cargo 其实一直没显示出来）。已在 `CuteCargoFlow` 和 `SupportLegs` 中补上。
- **迭代 v3（正交直线布局）**：按用户要求输送线改为直线 + 工厂式 90° 转弯。`createBeltPaths` 重写为轴对齐：H=(-1.2,h,0)、S=(8,h,0) 共线 z=0；purple 主干纯直线；green 南行 x=-6 后 90° 东转进 H；blue 出 H 北转 + 爬坡 + 90° 东转到包装床 z=3.6；peach/yellow 从 S 垂直 ±z 出料（交叉带分拣机式）。新增 `filletPolyline(points, r=0.45)`（内角 quadratic bezier 密化 5 段，r 钳制 0.45×min 段长防重叠）+ `beltCurve()` 助手，5 处曲线构建点统一替换，`clippedPoints` 先 clip 后 fillet，使 CatmullRom 直段保持笔直。CuteSorterJunction 的 heading fallback 改为 infeed 反向（±z 出料抵消时拨臂指向主干延伸方向）。相机预设同步更新。
- **迭代 v4（数据驱动仿真架构）**：按用户要求「哪里产生物料、设备提供运动路径、改变状态、后台记录、前台只渲染变化」新增 `src/sim/` 纯 TS 仿真内核（零 three 依赖，可进 Worker）：`types.ts`（Source/Transport/Junction/Sink 域模型 + SimEvent + FrameDelta 帧差量协议）、`pathMath.ts`（折线圆角 + 弧长表二分采样）、`FactorySim.ts`（设备图世界，tick→事件环形日志+差量；transport 带 minGap 防追尾自然排队；junction 按 routes 静态路由或 alternate 状态机；控制 API setRunning/setGlobalSpeed/setSourceRate）。已导出 `src/index.ts`。示例重构：`CuteCargoFlow`（前台随机动画）→ `SimCargoLayer`（消费 FrameDelta：spawned 分配槽位写色、moved 只写变化矩阵、removed 零缩停放回收，停机时零矩阵写入）；分拣机拨臂改由 sim routeIndex 驱动（最短弧插值）；新增 telemetry HUD（统计+最近 8 条事件，4Hz）；Leva cargoCount→spawnRate。注意：项目 tsconfig 开了 `noUncheckedIndexedAccess`，索引访问需判空。

